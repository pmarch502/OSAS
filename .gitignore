# Keep only the report site files
# Ignore intermediate/source data
pass2_combined.json
*.jsonl

# Working files - not for repo
no-commit/

# Ignore Claude project files
.claude/

# RCB build intermediates - the final {BOOK}.usfm carries everything
usfm/nt/*-pass1.usfm
usfm/nt/*-pass1-notes.md
usfm/nt/*-pass2.usfm
usfm/nt/*-pass2-*.usfm
usfm/nt/*-handoff.md
docs/rcb/data/*-pass1.js
docs/rcb/data/*-pass2.js

# The conversation records behind the exegesis - kept locally, not published
conversations/

# Framing audit output - reports only, never a source
audits/

# Python bytecode
__pycache__/
